services:
  - type: web
    name: meeting-vote
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATA_DIR
        value: /opt/render/project/src/data
      - key: DATABASE_URL
        value: ${databases.meeting-vote-db.connectionString}
    disk:
      mountPath: /opt/render/project/src/data

databases:
  - name: meeting-vote-db
    engine: postgresql
    plan: starter
    version: "14"
    diskSizeGb: 1
